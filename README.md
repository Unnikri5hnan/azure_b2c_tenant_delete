# azure_b2c_tenant_delete
Script can be used to delete an Azure AD B2C tenant and its associated resources. Before deleting the tenant, the script removes all dependent resources and clears the required prerequisites that would otherwise prevent the tenant from being removed.
